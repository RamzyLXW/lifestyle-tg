# STATUS — текущий срез (обновлено 2026-06-25)

> Короткая «где мы / что дальше». Полная карта — `README.md`, критерий отбора постов — `RUBRIC.md`.
> Возобновить сессию: открыть новый чат фразой **«продолжаем lifestyle tg, читай README.md, RUBRIC.md, STATUS.md»**.

## Что это
Телеграм-канал **«Тарас Шевченко. Лайфстайл»** (@taras_shevchenko_lifestyle, бот @book_lxw_book_bot):
раз в день — один цепляющий фрагмент Шевченко (украинский). Парсим litopys.org.ua ОДИН раз в свою базу
(`shevchenko.db`, SQLite), дальше живём из базы. Проект живёт в `~/lifestyle tg` (вне репо LuxWinWeb).

## Конвейер и статус
```
[импорт ✅] → [курирование ✅] → [вычитка владельцем 🔁] → [import:sheet ✅] → [post ✅] → [планировщик ✅ LIVE]
```
**Канал работает на автомате.** Единственная ручная задача владельца теперь — ставить `approved=1`
в Google Sheet; всё остальное (синк одобрений → выбор → постинг раз в день) делает CI.
- ✅ **Импорт поэзии** (Кобзар, тома 1–2): 222 стиха в базе (`kind=poem`).
- ✅ **Курирование** (Роль 2a, RUBRIC.md): сделано прямо в Claude Code (без API-ключа). Для каждого укр. стиха
  выбран `excerpt` + `source_label` + `footer_kind=source`, дословность проверена.
  Итог: **157 в пуле «берём» (`interesting=1`)**, 62 «мимо». 3 рус. стиха (shev121/123/151) ждут перевода.
- ✅ **Выгружено в Google Sheets** (221 строка), фильтр наведён на `interesting=1`.
- 🔁 **Вычитка владельцем — постоянный приток.** В таблице ставить `approved=1` на хороших; CI сам
  подтянет их в пул. Чем больше одобрено — тем дольше канал живёт без вмешательства (157 ≈ 5 мес.).
- ✅ **Планировщик (Роль 5) — LIVE, работает автономно.** Репо `github.com/RamzyLXW/lifestyle-tg` (private), 4 секрета.
  `.github/workflows/daily-post.yml`: cron `17 12 * * *` UTC + джиттер; на практике GitHub опаздывает ~2ч →
  пост падает **~17:xx по Киеву** (владелец принял; точное окно дал бы только внешний триггер). Цепочка:
  `import:sheet` → `post` → **надёжное** сохранение `published_at` в origin (переналожение на свежую БД + ретраи push).
- ✅ **Антиповтор в 2 слоя:** (1) точный по `source_id` навсегда; (2) кулдаун по ТЕКСТУ выжимки 90 дней
  (`COOLDOWN_DAYS`, разные цитаты из одного произведения не душатся). Постит ежедневно с 2026-06-25.
  ⚠️ Был инцидент 26→27.06: shev107 вышел дважды (коммит крона потерял гонку push) — **исправлено** (см. выше).
- ❌ Шаг перевода (3 рус. стиха / дневник) — не сделан (нужен `ANTHROPIC_API_KEY`).

## Что делать дальше (по шагам)
1. **(ВЛАДЕЛЕЦ, регулярно)** В таблице (SHEVCHENKO_SHEET_ID): фильтр по колонке **F `interesting`=1**
   (157 шт.) → на хороших ставить **`1` в колонке E `approved`**. Можно править `excerpt` (B) и
   `source_label` (C) прямо в ячейке. **Больше ничего делать не нужно** — CI каждый день сам
   подтянет одобрения и опубликует один новый стих. ⚠️ **Очередь почти пуста: остался shev237 (1 шт.)** —
   после ~29.06 постить будет нечего, нужно одобрить ещё в таблице.
2. **(опц.) Перевод** 3 рус. стихов (shev121/123/151) + дневника на украинский — нужен `ANTHROPIC_API_KEY`.
3. **(опц.) Ручной прогон/проверка:** Actions → daily-post → Run workflow (`dry_run=true` — показать без
   отправки; `dry_run=false` — внеплановый боевой пост). Локально: `npm run post -- --check` / `--dry-run`.

> Если когда-нибудь правишь БД/код локально — сначала `git pull` (CI пушет `shevchenko.db` после каждого
> поста), иначе разъедется. При работе только через таблицу локальный git трогать не нужно вообще.

## Ключевое
- **Секреты** в `.env`: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `SHEVCHENKO_SHEET_ID` (есть).
  Для перевода / повторного авто-курирования — добавить `ANTHROPIC_API_KEY` (сейчас НЕТ; курировал сам Claude Code).
- **Команды:** `npm run export` (БД→Sheets) · `import:sheet` (Sheets→БД) · `engine` (выбрать на день) ·
  `post [-- --check/--dry-run]` · `curate` (агент-куратор, нужен ключ) · `import:volume N` (докачать том) · `typecheck`.
- **Деньги/риск:** канал постит публично; перед `npm run post` (боевой) — `--dry-run`. Курирование в БД, не в проде.

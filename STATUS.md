# STATUS — текущий срез (обновлено 2026-06-25)

> Короткая «где мы / что дальше». Полная карта — `README.md`, критерий отбора постов — `RUBRIC.md`.
> Возобновить сессию: открыть новый чат фразой **«продолжаем lifestyle tg, читай README.md, RUBRIC.md, STATUS.md»**.

## Что это
Телеграм-канал **«Тарас Шевченко. Лайфстайл»** (@taras_shevchenko_lifestyle, бот @book_lxw_book_bot):
раз в день — один цепляющий фрагмент Шевченко (украинский). Парсим litopys.org.ua ОДИН раз в свою базу
(`shevchenko.db`, SQLite), дальше живём из базы. Проект живёт в `~/lifestyle tg` (вне репо LuxWinWeb).

## Конвейер и статус
```
[импорт ✅] → [курирование ✅] → [вычитка владельцем ⬅️ СЕЙЧАС] → [import:sheet] → [post] → [планировщик 🟡 код готов]
```
- ✅ **Импорт поэзии** (Кобзар, тома 1–2): 222 стиха в базе (`kind=poem`).
- ✅ **Курирование** (Роль 2a, RUBRIC.md): сделано прямо в Claude Code (без API-ключа). Для каждого укр. стиха
  выбран `excerpt` + `source_label` + `footer_kind=source`, дословность проверена.
  Итог: **157 в пуле «берём» (`interesting=1`)**, 62 «мимо». 3 рус. стиха (shev121/123/151) ждут перевода.
- ✅ **Выгружено в Google Sheets** (221 строка), фильтр наведён на `interesting=1`.
- ⬅️ **СЕЙЧАС: ручная вычитка владельцем** — в таблице проставить `approved=1`.
- 🟡 **Планировщик (Роль 5) — код готов.** `.github/workflows/daily-post.yml`: cron раз в день
  (12:00 UTC + джиттер → окно **14:00–16:00 по Киеву**), `npm run post`, коммит `shevchenko.db`
  обратно. Репо инициализирован локально (`git`, ветка `main`); остался **remote + секреты** (см. ниже).
- ❌ Шаг перевода (3 рус. стиха / дневник) — не сделан.

## Что делать дальше (по шагам)
1. **(ВЛАДЕЛЕЦ, сейчас)** В таблице (SHEVCHENKO_SHEET_ID, ссылка в выводе `npm run export`):
   фильтр по колонке **F `interesting`=1** (157 шт.) → на хороших ставить **`1` в колонке E `approved`**.
   Можно править `excerpt` (B) и `source_label` (C) прямо в ячейке.
   *По желанию: формат низа поста (одна строка vs перенос строк в стихе) — решить тут же.*
2. **`npm run import:sheet`** — вернуть одобрения (`approved=1` + правки) в базу.
3. **`npm run post -- --dry-run`** → **`npm run post`** — движок берёт ОДИН одобренный-неопубликованный
   стих, рендерит, шлёт в канал, помечает `published_at` (антиповтор). Одобряешь пачку — оно само
   раскидывает по одному в день; даты вручную не назначаешь. 157 ≈ 5 мес. ежедневных постов.
4. **Планировщик (Роль 5) — ДОДЕЛАТЬ remote.** Код есть; осталось включить на GitHub:
   - создать репо на GitHub и запушить (`git remote add origin … && git push -u origin main`);
   - в **Settings → Secrets and variables → Actions** добавить секреты:
     `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` (обязательно);
     `SHEVCHENKO_SHEET_ID` + `GOOGLE_SA_KEY` (весь JSON service-account) — опционально: если есть,
     CI сам тянет одобрения из таблицы и владелец работает ТОЛЬКО с Google Sheet (git не трогает);
   - **Actions → daily-post → Run workflow** с `dry_run=true` — проверить, что выбор/рендер ок, БЕЗ отправки;
     затем `dry_run=false` — боевой пост; дальше крутится по крону раз в день.
   - ⚠️ Перед включением: одобрить нужный батч в таблице (иначе первый пост = `shev104`, тест-стих).
5. **Перевод** 3 рус. стихов + (опц.) дневника на украинский — нужен `ANTHROPIC_API_KEY` в `.env`.

## Ключевое
- **Секреты** в `.env`: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `SHEVCHENKO_SHEET_ID` (есть).
  Для перевода / повторного авто-курирования — добавить `ANTHROPIC_API_KEY` (сейчас НЕТ; курировал сам Claude Code).
- **Команды:** `npm run export` (БД→Sheets) · `import:sheet` (Sheets→БД) · `engine` (выбрать на день) ·
  `post [-- --check/--dry-run]` · `curate` (агент-куратор, нужен ключ) · `import:volume N` (докачать том) · `typecheck`.
- **Деньги/риск:** канал постит публично; перед `npm run post` (боевой) — `--dry-run`. Курирование в БД, не в проде.
